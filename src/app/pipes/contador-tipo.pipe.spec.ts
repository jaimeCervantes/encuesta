import { ContadorTipoPipe } from './contador-tipo.pipe';

describe('ContadorTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new ContadorTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
